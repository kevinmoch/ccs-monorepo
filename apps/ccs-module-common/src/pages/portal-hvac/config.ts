import type { CardDefinition } from '@ccs/card';
export default {
  cards: [
    {
      id: 'portal-hvac',
      colSpan: { base: 12, md: 12 },
      props: { rootId: 'L2-10' }
    }
  ] satisfies CardDefinition[]
};
